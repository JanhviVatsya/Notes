We can generate all subsets (power set) of a string using bit manipulation.

#include <bits/stdc++.h>
using namespace std;

void generateSubsets(string s) {
    int n = s.size();
    int total = 1 << n;  // 2^n subsets

    for (int mask = 0; mask < total; mask++) {
        string subset = "";
        for (int j = 0; j < n; j++) {
            if (mask & (1 << j)) { // check if j-th bit is set
                subset += s[j];
            }
        }
        cout << subset << "\n";
    }
}

int main() {
    string s = "abc";
    generateSubsets(s);
    return 0;
}
